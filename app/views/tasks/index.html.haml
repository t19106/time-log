%h1 Tasks#index
%p Find me in app/views/tasks/index.html.haml

month:
= params[:month]
%br
- @tasks.each do |task|
  - day = task.date.day
  = link_to day, action: 'show', year: params[:year], month: params[:month], day: task.date.day
  %br
= link_to 'ログアウトする', logout_path
